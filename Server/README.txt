El programa está echo para ser ejecutado en diversos computadores, a menos que se cambie el puerto en el Server.java

1) Los mensajes del chat se pide hacerlos con un '_' o sin espacios, debido a que usa la función StringTokenizer y solo devuelve un token.
2) Se omitió el envió de archivos, debido al escaso tiempo de los integrantes.

OJO: Solo se muestran aquellos mensajes que contengan la misma ip que posea el computador en el cual se está ejecutando la aplicación.
-Enviar mensajes a la ip del computador en uso, para que este despues pueda ver aquellos mensajes.